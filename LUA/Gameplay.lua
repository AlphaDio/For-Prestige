
function AddCultureToLoyaltyPressure(player)

    print("AddCultureToLoyaltyPressure");

    -- Get all cities of the player
    local cities = player.GetCities();
    -- For each city
    for loop, city in ipairs(cities) do
        -- the city to which we add culture to loyalty
        if (city ~= nil) then

        end

    end

end

-- Get a new loyalty value using the Culture generated
function SetCityNewLoyalty(city, culture, newLoyalty)
    
end
